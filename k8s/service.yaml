apiVersion: v1
kind: Service
metadata:
  name: telecom-integration-checker-service
spec:
  selector:
    app: telecom-integration-checker
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
